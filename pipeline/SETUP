As root:

apt-get install python-pip build-essential lua5.1 liblua5.1-0-dev liblua5.1-socket2 liblua5.1-json python-dev git-core rsync screen libssl-dev
adduser archivebot

As user archivebot:

ssh-keygen
	[keep hitting Enter]
cat ~/.ssh/id_rsa.pub
	[send the public key to yipdw]
cd ~/
git clone https://github.com/ArchiveTeam/ArchiveBot
cd ArchiveBot
git submodule init
git submodule update
cd pipeline
pip install --user -r requirements.txt
./get-wget-lua.sh

As user archivebot, in first screen session:

ssh -C -L 127.0.0.1:16379:127.0.0.1:6379 yourusername@archivebot.at.ninjawedding.org -N

As user archivebot, in second screen session:

cd ~/ArchiveBot/pipeline
export RSYNC_URL=rsync://fos.textfiles.com/archivebot/
export LOG_CHANNEL=updates
export REDIS_URL=redis://127.0.0.1:16379/0
python ~/.local/bin/run-pipeline pipeline.py --disable-web-server --concurrent 2 ignorednick
